# Copy this file to `.env` and fill in secure values. DO NOT commit `.env`.

# --- Database Configuration ---
# Example values only — replace with strong secrets in your local `.env` file.
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=
POSTGRES_DB=secureguard_db

# --- Dependency-Track Database Configuration ---
# Use unique credentials for Dependency-Track if you run it.
ALPINE_DATABASE_URL=jdbc:postgresql://postgres-dtrack:5432/dtrack
ALPINE_DATABASE_USERNAME=your_dtrack_user
ALPINE_DATABASE_PASSWORD=

# --- App secrets / configuration ---
# Generate a strong secret for `SECRET_KEY` (e.g. `openssl rand -hex 32`)
SECRET_KEY=replace_with_a_strong_secret
JWT_SECRET=replace_with_a_strong_jwt_secret
ALLOWED_ORIGINS=http://localhost:3000

# --- Optional third-party keys (examples only) ---
# GOOGLE_SAFE_BROWSING_API_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_PRICE_PRO=
# STRIPE_PRICE_ENTERPRISE=

# Frontend configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
