# --- Database Configuration ---
POSTGRES_USER=secureuser
POSTGRES_PASSWORD=securepassword
POSTGRES_DB=secureguard_db

# --- Backend Configuration ---
DATABASE_URL=postgresql+asyncpg://secureuser:securepassword@db:5432/secureguard_db
# Generate a strong secret: openssl rand -hex 32
SECRET_KEY=your_production_secret_key_here
# Comma-separated list for CORS
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# API Key for Google Safe Browsing
GOOGLE_SAFE_BROWSING_API_KEY=mock
JWT_SECRET=your_jwt_secret_here

# --- Stripe Integration (Get these from your Dashboard) ---
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_ENTERPRISE=price_...

# --- Frontend Configuration ---
# Set this to the backend URL accessible by the browser
NEXT_PUBLIC_API_URL=http://localhost:8000
# Set this to your frontend URL (e.g., https://yourdomain.com)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
